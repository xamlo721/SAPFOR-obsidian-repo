Представляет выражение Fortran default.
```cpp
Represents Fortran default.

class **SgDefaultExp**: public SgExpression {
  // Fortran default node
  // variant == DEFAULT
public:
  SgDefaultExp(PTR_LLND ll);
  SgDefaultExp();
  ~SgDefaultExp();
};
```
#### Члены класса

`**SgDefaultExp**`**(PTR_LLND ll)**

document me

`**SgDefaultExp**`**()**

document me